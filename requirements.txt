# FLACSO — Estructura Social y Espacial de los Datos y su Arquitectura
# Requirements completo — sesiones 1 a 8

# ── Manipulación de datos ─────────────────────────────────
polars>=0.20.0
pandas>=2.0.0          # interoperabilidad con pyreadr y otras librerías
fastexcel>=0.20.2

# ── Lectura de formatos estadísticos ─────────────────────
pyreadr>=0.5.0         # lectura de .rds (R)
pyreadstat>=1.2.0      # lectura de .dta (Stata) y .sav (SPSS)

# ── Álgebra y estadística (sesión 5) ─────────────────────
svy        # estimación con diseño muestral complejo
statsmodels>=0.14.0    # modelos estadísticos generales
numpy>=1.26.0
scipy>=1.12.0          # t-test, chi-cuadrado, Mann-Whitney, KS, bootstrapping

# ── Visualización (sesión 6) ──────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
missingno>=0.5.2       # visualización de missings

# ── Redes (sesiones 7-8) ──────────────────────────────────
networkx>=3.2.0
neo4j>=5.0.0           # driver oficial para conectarse a Neo4j

# ── Conexión a PostgreSQL (sesiones 3-4 en adelante) ─────
psycopg2-binary>=2.9.0 # driver Postgres; -binary evita compilar desde fuente
sqlalchemy>=2.0.0      # ORM y conexión desde pandas/polars

# ── Entorno de trabajo ────────────────────────────────────
jupyter>=1.0.0